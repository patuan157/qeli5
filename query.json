{"Query 1": "select pub_type, count(id) as count\rfrom publication\rwhere year between 2000 and 2017\rgroup by pub_type;\r", "Query 2": "select distinct title, month, year\rfrom conference\rwhere month = 7 and id in (\r  select c.id\r  from conference c, inproceedings i\r  where i.conf_id = c.id\r  group by c.id\r  having count(c.id) > 200\r);\r", "Query 3a": "select p.id, p.title, p.month, p.year, p.pub_type, p.no_authors, p.decade\rfrom publication p, author a, link_publ_auth lpa\rwhere p.id = lpa.publ_id and a.id = lpa.auth_id and p.year = 2015 and a.id = 'X';\r", "Query 3b": "select p.id, p.title, p.month, p.year\rfrom publication p, author a, link_publ_auth lpa\rwhere p.id = lpa.publ_id and a.id = lpa.auth_id and split_part(p.id, '/', 2) = 'Z' and p.year = Y and a.id = 'X';\r", "Query 3c": "select a.id\rfrom publication p, author a, link_publ_auth lpa\rwhere p.id = lpa.publ_id and a.id = lpa.auth_id and split_part(p.id, '/', 2) = 'Z' and p.year = Y\rgroup by a.id\rhaving count(a.id) >= 2;\r", "Query 4a": "select a.id\rfrom publication p, author a, link_publ_auth lpa \rwhere p.id = lpa.publ_id and a.id = lpa.auth_id and split_part(p.id, '/', 2) = 'pvldb'\rgroup by a.id\rhaving count(a.id) >= 10\rintersect \rselect a.id\rfrom publication p, author a, link_publ_auth lpa \rwhere p.id = lpa.publ_id and a.id = lpa.auth_id and split_part(p.id, '/', 2) = 'sigmod'\rgroup by a.id\rhaving count(a.id) >= 10;\r", "Query 4b": "select a.id\rfrom publication p, author a, link_publ_auth lpa \rwhere p.id = lpa.publ_id and a.id = lpa.auth_id and split_part(p.id, '/', 2) = 'pvldb'\rgroup by a.id\rhaving count(a.id) >= 15\rexcept\rselect a.id\rfrom publication p, author a, link_publ_auth lpa \rwhere p.id = lpa.publ_id and a.id = lpa.auth_id and split_part(p.id, '/', 2) = 'kdd'\rgroup by a.id;\r", "Query 5": "select decade, count(id)\rfrom publication p\rgroup by decade\rorder by decade;\r", "Query 6": "create view data_max as\rselect p.id, p.no_authors\rfrom publication p, conference c\rwhere split_part(p.id, '/', 2) = split_part(c.key, '/', 2) and p.month = c.month and p.year = c.year and lower(c.title) like '%data%'\runion\rselect p.id, p.no_authors\rfrom publication p, journal j\rwhere split_part(p.id, '/', 2) = j.code and p.month = j.month and p.year = j.year and lower(j.title) like '%data%';\r\rselect a.id, p.id, p.no_authors\rfrom author a, link_publ_auth lpa, publication p\rwhere lpa.publ_id = (\r  select id\r  from data_max\r  where no_authors = (\r    select max(no_authors)\r    from data_max\r  )\r) and a.id = lpa.auth_id and lpa.publ_id = p.id;\r", "Query 7": "create view data_pub as\rselect p.id\rfrom publication p, conference c\rwhere split_part(p.id, '/', 2) = split_part(c.key, '/', 2) and p.month = c.month and p.year = c.year and lower(c.title) like '%data%' and date_part('year', CURRENT_DATE) - p.year <= 5\runion\rselect p.id\rfrom publication p, journal j\rwhere split_part(p.id, '/', 2) = j.code and p.month = j.month and p.year = j.year and lower(j.title) like '%data%' and date_part('year', CURRENT_DATE) - p.year <= 5;\r\rselect a.id, count(a.id)\rfrom data_pub d, author a, link_publ_auth lpa\rwhere a.id = lpa.auth_id and d.id = lpa.publ_id\rgroup by a.id\rorder by count(a.id) desc\rlimit 10;\r", "Query 8": "select distinct c.title\rfrom publication p, conference c\rwhere split_part(p.id, '/', 2) = split_part(c.key, '/', 2) and p.month = 6 and p.year = c.year\rgroup by c.id\rhaving count(c.id) >= 100;\r", "Query 9a": "select a.id\rfrom author a \rwhere lower(a.family_name) like 'h%' and 31 = (\r  select count(*)\r  from (\r    select distinct p.year\r    from publication p, author a1, link_publ_auth lpa \r    where a1.id = lpa.auth_id and p.id = lpa.publ_id and a1.id = a.id and date_part('year', CURRENT_DATE) - p.year <= 30\r  ) as tmp\r);\r", "Query 9b": "select a.id, count(*)\rfrom publication p, author a, link_publ_auth lpa, (\r  select year, min(month) as month\r  from publication\r  group by year\r  order by year\r  limit 1\r) as tmp\rwhere a.id = lpa.auth_id and p.id = lpa.publ_id and p.year = tmp.year and p.month = tmp.month\rgroup by a.id;\r", "Query 10": "select a.id\rfrom publication p, author a, link_publ_auth lpa\rwhere p.id = lpa.publ_id and a.id = lpa.auth_id and p.pub_type = 'inproceedings' and date_part('year', CURRENT_DATE) - p.year <= 5\rexcept\rselect a.id\rfrom publication p, author a, link_publ_auth lpa\rwhere p.id = lpa.publ_id and a.id = lpa.auth_id and p.pub_type = 'article' and date_part('year', CURRENT_DATE) - p.year <= 5;\r"}